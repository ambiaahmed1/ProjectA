name: Trigger Workflow in Repository B

on:
  push:
    branches:
      - main

jobs:
  trigger:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger Workflow in Repository A
        run: |
          curl -X POST -H "Authorization: token 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" -H "Accept: application/vnd.github.everest-preview+json" https://api.github.com/repos/ambiaahmed1/ProjectB/dispatches --data "{\"event_type\": \"trigger-external-workflow\"}"



